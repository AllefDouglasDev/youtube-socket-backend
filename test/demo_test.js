const assert = require('assert');

// Describe tests
describe("Some demo tests", function() {

    // Create tests
    it("Adds two numbers together", function() {
        assert(2 + 3 === 5);
    })
});